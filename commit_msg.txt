fix: remove trailingSlash (caused cached 308 redirects breaking session fetch)

The trailingSlash: true config caused Chrome to cache 308 Permanent Redirects
on /api/auth/* routes (from disk cache), which broke the session fetch chain.
Also added Cache-Control: no-store headers on all auth endpoints as a safety net.
