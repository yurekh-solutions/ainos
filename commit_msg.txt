Blog cards show content preview + AI generation quota resilience + real photos

Cards:
- Queued (Preparing) cards now show a readable content preview built
  from niche + keywords, so every card shows heading, content, image
- Recent Blog Activity no longer lists CANCELLED rows from old
  disconnected sites

Generation resilience (Gemini free quota 429 / Pollinations 402):
- Multiple Gemini keys rotate on quota errors: GEMINI_API_KEYS="k1,k2"
- Quota/rate-limit errors keep rows pending ("Preparing") instead of
  FAILED and the batch stops instead of burning attempts; rows retry
  automatically on the next run / daily quota reset
- Non-JSON AI replies are salvaged as markdown articles instead of failing
- Failure reasons are now logged server-side

Images (Pollinations rejected as low quality):
- Per-topic hash picks a distinct Pexels photo per blog (Date-based
  seed previously gave identical images to blogs created in one loop)
- Publish step prefers a fresh real photo when Pexels/Unsplash is configured
- DB fix applied: all 29 published posts with Pollinations images
  replaced with topic-matched Pexels photos
