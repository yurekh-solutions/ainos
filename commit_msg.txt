fix: auth flow - remove strict provider check, fix session timing, remove duplicate auth guards

- signIn callback: removed strict provider check that was blocking sign-in
- signIn callback: now accepts any OAuth provider with valid email
- session callback: removed async DB enrichment (was causing timing issues)
- session callback: now builds session purely from JWT synchronously
- dashboard page: removed duplicate auth guard (ProtectedRoute handles it)
- dashboard page: replaced CSS variable spinner with hardcoded Tailwind colors
- dashboard page: uses simple bg-gray-50 instead of CSS variable gradients
- These fixes together resolve the redirect loop and blank page issues after Google sign-in
