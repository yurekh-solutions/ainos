fix: blank dashboard page - invisible spinner and hanging auth check

- Spinner used undefined CSS variables (var(--page-gradient), var(--border), var(--primary))
- Made spinner visible with hardcoded colors (bg-gray-50, border-gray-200, border-t-purple-600)
- Added 5-second timeout to force redirect if session check hangs
- Separated loading and unauthenticated states for better UX
- Prevents blank page when SessionProvider can't reach auth endpoint