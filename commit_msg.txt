feat(blogs): Generate Now for scheduled blogs + reader featured image

- schedule-generator.ts: shared per-schedule generation (AI content + Pexels image + quota)
- POST /api/blog-agent/generate-now: on-demand generation of a scheduled blog
- auto-generate cron refactored to use the shared generator
- blog reader: featured image shown; scheduled posts get a clear "queued" state with a Generate Now with AI button instead of empty body
- blog cards: Queued chip + Generate Now (Zap) action for schedules
- blog-agent: first scheduled blog auto-generates in background right after connecting a website
