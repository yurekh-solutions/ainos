Early-access free mode: all tools free until BILLING_ENFORCED=true

- billingEnforced() flag in lib/billing.ts; checkQuota bypasses all limits
  (no DB hit) while billing is off
- /api/billing GET reports enforced flag + unlimited limits in free mode
- Billing page: early-access banner, plan cards show 'Free during early
  access' instead of upgrade buttons until billing goes live
- Services page chip: 'Early access - ALL TOOLS FREE' badge
- Flip to paid later by setting BILLING_ENFORCED=true (+ Razorpay keys)
  in Render env - quotas and checkout activate instantly, no code change
