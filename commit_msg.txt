fix(embed): multi-tenant isolation - widget serves only its own website's blogs

- embed.js sends site=<current url>; blog-embed API matches host against active connected websites and filters posts by that company
- unknown/unmatched sites get an empty list - no cross-client blog leakage
