fix: completely break sign-in/dashboard redirect loop

- Removed ALL auto-redirect from sign-in page
- Sign-in page now always shows the Google button
- Removed 5-second loading timeout from dashboard that caused loop
- Dashboard only redirects when session status is definitely 'unauthenticated'
- Loading state shows visible spinner without redirecting
- This prevents the pages from flashing/looping between each other