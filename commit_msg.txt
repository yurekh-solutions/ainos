fix: break redirect loop between sign-in and dashboard

- Changed sign-in page redirect from router.replace('/') to window.location.href('/ainos/')
- Added session.user.email validation before redirecting (prevents loop with invalid sessions)
- Full page navigation breaks the client-side routing loop
- Dashboard still redirects unauthenticated users to sign-in