fix(blogs): card images, working delete for queued blogs, always-visible actions, Generate All

- blog cards now show featured image (branded gradient placeholder for queued blogs)
- action buttons (view/regenerate/generate-now/delete) always visible, not hover-only
- DELETE /api/blog-posts now also deletes BlogSchedule rows (queued blogs) — delete was silently failing on schedules
- Generate All Queued button: bulk-generates every scheduled blog sequentially with live progress (full 3000-word articles + premium images)
